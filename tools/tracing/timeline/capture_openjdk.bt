usdt:$MMTK:mmtk_openjdk:code_cache_roots {
    if (@enable_print) {
        printf("code_cache_roots,meta,%d,%lu,%lu,%lu,%lu\n", tid, nsecs, arg0, arg1, arg2);
    }
}

usdt:$MMTK:mmtk_openjdk:fix_relocations {
    if (@enable_print) {
        printf("fix_relocations,meta,%d,%lu,%lu\n", tid, nsecs, arg0);
    }
}
